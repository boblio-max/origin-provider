# origin-provider

An OpenAI-compatible model provider server (Python standard library only),
built to work with [OpenCode](https://opencode.ai/docs/providers/#custom-provider)
as a custom provider.

Status: scaffolding — `server.py` (the stub: `POST /v1/chat/completions`,
`GET /v1/models`, Bearer auth, SSE streaming, tool-call round trip) is next.
